# Question:
# Create a 3×3 matrix (nested list) and print it.

matrix_list = [[1, 2, 3], [4, 5, 6], [7, 8, 9]] # Creates matrix list

for row in matrix_list: # Loops through the matrix list array and prints the rows
    print(row)